<!--index.wxml-->
<view class="container">
  <view class="item"  wx:for="{{movies}}" wx:key="{{item}}" bind:tap="handleClick" data-id = "{{item.id}}">
    <view>
      <image src="{{item.imgUrl}}"></image>
    </view>
    <view>{{item.title}}</view>
  </view>
</view>
